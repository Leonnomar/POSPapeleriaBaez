<UserControl x:Class="PapeleriaBaez.Views.InventarioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:PapeleriaBaez.Views"
             mc:Ignorable="d">
    <Grid Margin="15">

        <Grid.RowDefinitions>

            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="📦 Inventario"
                   FontSize="28"
                   FontWeight="Bold"/>

        <Grid Grid.Row="1" Margin="0,15">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Buscar:"
                       VerticalAlignment="Center"/>

            <TextBox x:Name="txtBuscar"
                     Grid.Column="1"
                     Height="30"
                     Margin="10,0"
                     TextChanged="txtBuscar_TextChanged"/>

            <WrapPanel Grid.Column="3"
                       HorizontalAlignment="Left">

                <Button Content="📦 Todos"
                        Width="120"
                        Margin="5"
                        Click="BtnTodos_Click"/>

                <Button Content="🟢 Disponibles"
                        Width="120"
                        Margin="5"
                        Click="BtnDisponibles_Click"/>

                <Button Content="🟡 Stock Bajo"
                        Width="120"
                        Margin="5"
                        Click="BtnStockBajo_Click"/>

                <Button Content="🔴 Agotados"
                        Width="120"
                        Margin="5"
                        Click="BtnAgotados_Click"/>

            </WrapPanel>

        </Grid>

        <DataGrid x:Name="dgInventario"
                  Grid.Row="2"
                  AutoGenerateColumns="False">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Código"
                                    Binding="{Binding Codigo}"/>

                <DataGridTextColumn Header="Producto"
                                    Binding="{Binding Nombre}"
                                    Width="*"/>

                <DataGridTextColumn Header="Categoría"
                                    Binding="{Binding Categoria}"/>

                <DataGridTextColumn Header="Stock"
                                    Binding="{Binding Stock}"/>

                <DataGridTextColumn Header="Mínimo"
                                    Binding="{Binding StockMinimo}"/>

                <DataGridTemplateColumn Header="Estado" Width="120">

                    <DataGridTemplateColumn.CellTemplate>

                        <DataTemplate>

                            <Border x:Name="bdEstado"
                                    Padding="5"
                                    CornerRadius="4"
                                    HorizontalAlignment="Center">

                                <TextBlock x:Name="txtEstado"
                                           Text="{Binding Estado}"
                                           FontWeight="Bold"
                                           HorizontalAlignment="Center"/>

                            </Border>

                            <DataTemplate.Triggers>

                                <DataTrigger Binding="{Binding Estado}"
                                             Value="Disponible">

                                    <Setter TargetName="bdEstado"
                                            Property="Background"
                                            Value="#C8E6C9"/>

                                    <Setter TargetName="txtEstado"
                                            Property="Foreground"
                                            Value="#1B5E20"/>
                                    
                                </DataTrigger>

                                <DataTrigger Binding="{Binding Estado}"
                                             Value="Stock Bajo">

                                    <Setter TargetName="bdEstado"
                                            Property="Background"
                                            Value="#FFF9C4"/>

                                    <Setter TargetName="txtEstado"
                                            Property="Foreground"
                                            Value="#F57F17"/>
                                    
                                </DataTrigger>

                                <DataTrigger Binding="{Binding Estado}"
                                             Value="Agotado">

                                    <Setter TargetName="bdEstado"
                                            Property="Background"
                                            Value="#FFCDD2"/>

                                    <Setter TargetName="txtEstado"
                                            Property="Foreground"
                                            Value="#B71C1C"/>
                                    
                                </DataTrigger>
                                
                            </DataTemplate.Triggers>
                            
                        </DataTemplate>
                        
                    </DataGridTemplateColumn.CellTemplate>
                    
                </DataGridTemplateColumn>
                
                <DataGridTextColumn Header="Valor"
                                    Binding="{Binding ValorInventario,
                                              StringFormat={}{0:C}}"/>

            </DataGrid.Columns>

        </DataGrid>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15">

            <TextBlock x:Name="lblResumen"/>

        </StackPanel>

    </Grid>

</UserControl>
