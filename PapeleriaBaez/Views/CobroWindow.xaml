<Window x:Class="PapeleriaBaez.Views.CobroWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PapeleriaBaez.Views"
        mc:Ignorable="d"
        Title="Cobrar Venta"
        Height="520"
        Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    
    <Grid Margin="20">

        <Grid.RowDefinitions>

            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            
        </Grid.RowDefinitions>

        <TextBlock Text="COBRAR VENTA"
                   FontSize="28"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="1"
                    Margin="0,30">

            <TextBlock Text="Total"
                       FontSize="18"/>

            <TextBlock x:Name="lblTotal"
                       FontSize="42"
                       FontWeight="Bold"
                       Foreground="DarkGreen"
                       HorizontalAlignment="Center"/>
            
        </StackPanel>

        <GroupBox Header="Método de pago"
                  Grid.Row="2"
                  Margin="0,20">

            <StackPanel Margin="10">

                <RadioButton x:Name="rbEfectivo"
                             Content="Efectivo"
                             IsChecked="True"/>

                <RadioButton x:Name="rbTarjeta"
                             Content="Tarjeta"/>

                <RadioButton x:Name="rbTransferencia"
                             Content="Transferencia"/>

                <RadioButton x:Name="rbMixto"
                             Content="Pago Mixto"/>
                
            </StackPanel>
            
        </GroupBox>

        <Grid Grid.Row="3"
              Margin="0,20">

            <Grid.ColumnDefinitions>

                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="180"/>

            </Grid.ColumnDefinitions>

            <TextBlock Text="Recibido:"
                       VerticalAlignment="Center"/>

            <TextBox x:Name="txtRecibido"
                     Grid.Column="1"
                     Height="35"
                     FontSize="20"
                     TextChanged="txtRecibido_TextChanged"/>
            
        </Grid>

        <StackPanel Grid.Row="4"
                    VerticalAlignment="Bottom">

            <TextBlock Text="Cambio"
                       FontSize="18"/>

            <TextBlock x:Name="lblCambio"
                       FontSize="36"
                       FontWeight="Bold"
                       Foreground="Blue"
                       Margin="0,0,0,20"/>

            <Button x:Name="btnCobrar"
                    Content="CONFIRMAR VENTA"
                    Height="50"
                    FontSize="18"
                    Click="btnCobrar_Click"/>
            
        </StackPanel>
        
    </Grid>
    
</Window>
